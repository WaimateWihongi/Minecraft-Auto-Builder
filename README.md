# Minecraft-Auto-Builder

# How it works
- It uses a text file that contains the blocks that constructs the structure
- The script then parses the text file and builds the structure
# Requirements
- Python 3.7
# Packages
- tkinter 
- keyboard
